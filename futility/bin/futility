#!/bin/bash

if [[ -n FUTILITY_VERSION ]]; then
  # For now this is a bug.
  echo 'FUTILITY_VERSION has been set. Something is loading this twice!' 1>&2
else
  source $(dirname $BASH_SOURCE)/../package.sh
fi

