#!/usr/bin/env bash

if [[ -n $(type -t load_futility_package) ]]; then
  # For now this is a bug.
  echo 'load_futility_package has been set. Something is loading this twice!' 1>&2
else
  source $(dirname $BASH_SOURCE)/../package.sh
fi

