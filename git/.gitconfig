[user]
    name = Sebastian Sangervasi
[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
    liff = !git log \
            -n 10 --oneline --first-parent --decorate \
            --left-right
    lg = log \
            --branches --tags --graph \
            --date=relative \
            --pretty=format:\"%h  %ad  %C(red)%ae%Creset%x09%C(green)%d %Creset%s\" \
            -n 50

[push]
    default = simple
